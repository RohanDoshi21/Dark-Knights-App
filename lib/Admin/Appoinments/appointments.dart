import 'package:flutter/material.dart';

class AppointmentsHomeAdmin extends StatelessWidget {
  const AppointmentsHomeAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is the Appoinments Page!"),
    );
  }
}

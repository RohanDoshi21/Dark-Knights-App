import 'package:flutter/material.dart';

class FileHomeAdmin extends StatelessWidget {
  const FileHomeAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is the Files page!"),
    );
  }
}
